define ->
  (match) ->
    match '', 'main#index', name: 'index'
    # match 'profile', 'main#profile', name: 'profile'
    # match 'login', 'oauth#login', name: 'login'
    # match 'logout', 'oauth#logout', name: 'logout'
    # match 'callback', 'oauth#callback'
    # match 'endpoint', 'oauth#endpoint'
